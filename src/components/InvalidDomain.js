import React, { Component } from "react";

const InvalidDomain = (props) =>  {

    return (
        <div className="uxt-wa-invalid-domain">
            <p>Unauthorized User <br /> Please check our website to subscribe.</p>
        </div>
    );
};

export default InvalidDomain;